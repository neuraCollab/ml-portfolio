apiVersion: v1
kind: Service
metadata:
  name: cassandra-grpc-ml-minio
spec:
  selector:
    app: cassandra-grpc-ml-minio
  ports:
    - name: s3-api
      port: 9000
      targetPort: 9000
  type: ClusterIP
